# deepbleed_xnat

Deepbleed container for XNAT

## Status
There should be some problem in the script, because the result is not correct. But the container runs on XNat.

## Usage
from XNAT Administer/PluginSettings/Images&Commands/NewImage
Input: `0rc0/deepbleed_xnat`as the image name and `:latest` as the version
Under `New Command` cut and paste the content of the JSON file.